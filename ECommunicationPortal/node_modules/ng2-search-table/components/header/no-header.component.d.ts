import { TableHeader } from "./table-header";
export declare class NoHeaderComponent implements TableHeader {
    name: string;
    model: any;
    eventEmitter: any;
    isHidden: boolean;
    setVisibility(visible: boolean): void;
}
