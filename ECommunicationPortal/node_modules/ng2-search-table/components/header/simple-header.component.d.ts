import { TableHeader } from "./table-header";
export declare class SimpleHeaderComponent implements TableHeader {
    name: string;
    model: any;
    eventEmitter: any;
    isHidden: boolean;
    setVisibility(visible: boolean): void;
}
