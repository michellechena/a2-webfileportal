import { OnInit, Compiler, ComponentFactoryResolver } from "@angular/core";
import { SearchTableService } from "../services/search-table.service";
export declare class SearchTableComponent implements OnInit {
    private componentResolver;
    private searchTableService;
    private compiler;
    headerViewComponents: any;
    headerFilterComponents: any;
    tableClass: string;
    columns: any[];
    config: any;
    dataRows: any[];
    headerComponents: any[];
    private sortCondition;
    private filterConditions;
    private currentPage;
    private pagePer;
    private totalCount;
    private debounceSearchEventEmitter;
    private debounceTime;
    private headerInstances;
    private filterInstances;
    private defaultValues;
    private visibilities;
    constructor(componentResolver: ComponentFactoryResolver, searchTableService: SearchTableService, compiler: Compiler);
    ngOnInit(): void;
    getCurrentPage(): number;
    getPagePer(): number;
    getTotalCount(): number;
    setCurrentPage(page: number): void;
    setPagePer(per: number): void;
    search(): void;
    setSortDirection(name: string, direction: string): void;
    setFilterValue(name: string, value: any, subComponentName?: string): void;
    setVisibility(name: string, visible: boolean): void;
    private parseConfig(config);
    private setTotalCount(count);
    private resetCurrentPage();
    private createHeaderTableComponent(factory, header, viewComponents);
    private createFilterTableComponent(factory, header, viewComponents);
    private clearHeaderSortDirection(without?);
}
